# TOOLS.md

Use read/search tools to inspect the packet. Prefer non-destructive operations.

If routing this packet onward, preserve provenance and include `docs/04_INGESTION_PROMPT.md`.

Do not post publicly unless operating under a concrete user-specified destination or workflow.
